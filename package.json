{
  "devDependencies": {
    "@parcel/transformer-elm": "^2.12.0",
    "elm": "^0.19.1-5",
    "parcel": "^2.12.0",
    "serve": "^14.2.3"
  },
  "scripts": {
    "build": "parcel build src/index.html",
    "dev": "parcel src/index.html",
    "serve": "parcel build src/index.html && serve dist -p 1234",
    "clean": "rm -rf .parcel-cache dist"
  }
}
